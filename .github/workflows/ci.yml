name: Mobile Tests CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  android-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Install Appium and UIAutomator2 Driver
        run: |
          npm install -g appium
          # Verifica se o driver está instalado
          if ! appium driver list --installed | grep uiautomator2; then
            echo "uiautomator2 não instalado. Instalando..."
            appium driver install uiautomator2@6.1.1 --source=npm
          else
            echo "uiautomator2 já instalado. Atualizando para a versão mínima compatível..."
            appium driver update uiautomator2@6.1.1 --source=npm
          fi

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Create Emulator
        run: |
          echo "no" | avdmanager create avd -n test -k "system-images;android-30;google_apis;x86"

      - name: Start Emulator
        run: |
          nohup emulator -avd test -no-snapshot-load -no-window &
          adb wait-for-device
          adb shell input keyevent 82

      - name: Run Tests
        run: npx wdio run wdio.conf.js
